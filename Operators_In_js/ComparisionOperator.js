// 1. Description: Create a JavaScript program to greet the user by their name.
// Suggested Name: "Name Greeting"

var NameGreeting = "Hello Everyone";
console.log(NameGreeting)

//2. Problem: 2 -- Description: Develop a program to calculate the area of a rectangle using
// user-provided dimensions.
// Suggested Name: "Rectangle Area Calculator"

    var length = 10;
    var bredth = 20;
    RectangleArea = (length*bredth)
    console.log(RectangleArea)

//  3) escription: Write a program to compare two numbers and determine if
// they are equal.
// Suggested Name: "Number Comparison"
       
        var a = 43;
        var b = 32;
        // NumberComparison= (a>=b)
        NumberComparison= (a<=b)
        console.log(NumberComparison)
       
// 4)  Description: Create a program to check if a user is eligible to vote based on
// their age.
// Suggested Name: "Voting Eligibility Checker"
           
        var Age =18;
        // VotingEligibilityChecker = (Age<=17 )
        VotingEligibilityChecker = (Age>=19)
        console.log(VotingEligibilityChecker)

        
// prob : 5-- Description: Design a program to evaluate if a user can afford items within a
// specified budget.
// Suggested Name: "Budget Shopping Evaluation"

var Budget = 50000
BudgetShoppingEvaluation = (Budget>=10998)
console.log(BudgetShoppingEvaluation)

