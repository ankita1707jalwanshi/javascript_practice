// 6. Write a program to reverse a given string using a for loop (without using
// built-in reverse functions).

var name = "abcd";
var reverse = "";
for(var i="abcd".length - 1;i >=0;i--){
    reverse = reverse +"abcd"[i];
}
console.log(reverse);

