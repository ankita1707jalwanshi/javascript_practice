// Proble-1 : Voting Eligibility: Write a program that checks whether a person can vote.
// To be eligible, a person must be at least 18 years old and a citizen of the
// country.

var age = 18;
if(age>18){
    if(citizen === country){
        console.log("They can vote")
    }
    
}
else{
    console.log("They can  not vote") 
}